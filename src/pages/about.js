import { Button } from 'reactstrap';
import Base from './Base';

const About = () => {
return(

    <Base>
        <h1> This is about page</h1>
            we are building website that can communicate with the spring boot.
               
    </Base>

);
}

export default About;